; Заготовка для рисования цветом
; -------------------------- тест
    mov  dx, 3CEh
    mov  ax, 0x0205 ; 05h = Регистр режима, 2
    out  dx, ax

    mov  ax, 0xFF08 ; 08h (Режим), AH=Битовая маска 0xFF
    out  dx, ax

    ; Пишется цвет (AL-цвет)
    mov al, 0xE
    mov [0xa0000+80*50+40], al
    mov al, [0xa0000]
; --------------------------
